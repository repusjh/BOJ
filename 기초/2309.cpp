#include <bits/stdc++.h>
using namespace std;

#define ARRAY_SIZE 9

int height[ARRAY_SIZE];

void findNanjangs(int sum, int chosen){
    
}

int main(){
    for (int i = 0; i < ARRAY_SIZE; i++){
        cin >> height[i];
    }

    sort(height, height + ARRAY_SIZE);
}